type Token @entity(immutable: false) {
  # token address
  id: ID!
  # token symbol
  symbol: String!
  # token name
  name: String!
  # token decimals
  decimals: BigInt!
  # token total supply
  totalSupply: BigInt!
  # volume in token units
  volume: BigDecimal!
  # volume in derived USD
  volumeUSD: BigDecimal!
  # volume in USD even on pools with less reliable USD values
  untrackedVolumeUSD: BigDecimal!
  # fees in USD
  feesUSD: BigDecimal!
  # transactions across all pools that include this token
  txCount: BigInt!
  # number of pools containing this token
  poolCount: BigInt!
  # liquidity across all pools in token units
  totalValueLocked: BigDecimal!
  # liquidity across all pools in derived USD
  totalValueLockedUSD: BigDecimal!
  # TVL derived in USD untracked
  totalValueLockedUSDUntracked: BigDecimal!
  # Note: for chains where IP is not the native token, this will be the derived
  # price of that chain's native token, effectively, this should be renamed
  # derivedNative
  derivedIP: BigDecimal!
  # pool currently used as the best whitelist pricing source
  pricingPool: String
  # cached native liquidity for the current pricing pool
  pricingPoolLiquidityIP: BigDecimal!
  # pools token is in that are white listed for USD pricing
  whitelistPools: [Pool!]!
  # neighbours token for swap routing
  neighbour: [Token!]!
}

type Pool @entity(immutable: false) {
  # pool address
  id: ID!
  # creation
  createdAtTimestamp: BigInt!
  # block pool was created at
  createdAtBlockNumber: BigInt!
  # token0
  token0: Token!
  # token1
  token1: Token!
  # fee amount
  feeTier: BigInt!
  # in range liquidity
  liquidity: BigInt!
  # current price tracker
  sqrtPrice: BigInt!
  # token0 per token1
  token0Price: BigDecimal!
  # token1 per token0
  token1Price: BigDecimal!
  # current tick
  tick: BigInt
  # current observation index
  observationIndex: BigInt!
  # all time token0 swapped
  volumeToken0: BigDecimal!
  # all time token1 swapped
  volumeToken1: BigDecimal!
  # all time USD swapped
  volumeUSD: BigDecimal!
  # all time USD swapped, unfiltered for unreliable USD pools
  untrackedVolumeUSD: BigDecimal!
  # fees in USD
  feesUSD: BigDecimal!
  # all time number of transactions
  txCount: BigInt!
  # all time fees collected token0
  collectedFeesToken0: BigDecimal!
  # all time fees collected token1
  collectedFeesToken1: BigDecimal!
  # all time fees collected derived USD
  collectedFeesUSD: BigDecimal!
  # total token 0 across all ticks
  totalValueLockedToken0: BigDecimal!
  # total token 1 across all ticks
  totalValueLockedToken1: BigDecimal!
  # tvl derived IP
  totalValueLockedIP: BigDecimal!
  # tvl USD
  totalValueLockedUSD: BigDecimal!
  # TVL derived in USD untracked
  totalValueLockedUSDUntracked: BigDecimal!
  # Fields used to help derived relationship
  liquidityProviderCount: BigInt! # used to detect new exchanges
  # daily snapshots of pool data
  poolDayData: [PoolDayData!]! @derivedFrom(field: "pool")
  # derived fields
  swaps: [Swap!]! @derivedFrom(field: "pool")
  from: String
  feesIP: BigDecimal!
  feeAPRUSD: BigDecimal!
  feeAPRIP: BigDecimal!
}

type Swap @entity(immutable: true) {
  # transaction hash + "#" + index in swaps Transaction array
  id: ID!
  # transaction hash
  txHash: String!
  # timestamp of transaction
  timestamp: BigInt!
  # pool swap occured within
  pool: Pool!
  # allow indexing by tokens
  token0: Token!
  # allow indexing by tokens
  token1: Token!
  # sender of the swap
  sender: Bytes!
  # recipient of the swap
  recipient: Bytes!
  # txn origin
  origin: Bytes! # the EOA that initiated the txn
  # delta of token0 swapped
  amount0: BigDecimal!
  # delta of token1 swapped
  amount1: BigDecimal!
  # derived info
  amountUSD: BigDecimal!
  # The sqrt(price) of the pool after the swap, as a Q64.96
  sqrtPriceX96: BigInt!
  # the tick after the swap
  tick: BigInt!
  # index within the txn
  logIndex: BigInt
}

# Data accumulated and condensed into day stats for each pool
type PoolDayData @entity(immutable: false) {
  # timestamp rounded to current day by dividing by 86400
  id: ID!
  # timestamp rounded to current day by dividing by 86400
  date: Int!
  # pointer to pool
  pool: Pool!
  # in range liquidity at end of period
  liquidity: BigInt!
  # current price tracker at end of period
  sqrtPrice: BigInt!
  # price of token0 - derived from sqrtPrice
  token0Price: BigDecimal!
  # price of token1 - derived from sqrtPrice
  token1Price: BigDecimal!
  # current tick at end of period
  tick: BigInt
  # tvl derived in USD at end of period
  tvlUSD: BigDecimal!
  # volume in token0
  volumeToken0: BigDecimal!
  # volume in token1
  volumeToken1: BigDecimal!
  # volume in USD
  volumeUSD: BigDecimal!
  # fees in USD
  feesUSD: BigDecimal!
  # numebr of transactions during period
  txCount: BigInt!
  # opening price of token0
  open: BigDecimal!
  # high price of token0
  high: BigDecimal!
  # low price of token0
  low: BigDecimal!
  # close price of token0
  close: BigDecimal!
}

type PriceState @entity(immutable: false) {
  id: ID!
  ipPriceUSD: BigDecimal!
  sourcePool: String!
  updatedAtBlock: BigInt!
  updatedAtTimestamp: BigInt!
}

type IpTokenLink @entity(immutable: true) {
  id: ID!
  ipaId: String!
  token: String!
  blockNumber: BigInt!
  timestamp: BigInt!
  transactionHash: String!
}

type TokenDeployment @entity(immutable: true) {
  id: ID!                      # token address
  blockNumber: BigInt!
  timestamp: BigInt!
  transactionHash: String!
  tokenCreator: String!
  token: String!
  pool: String!
  startTickList: [BigInt!]!
  allocationList: [BigInt!]!
}

type TokenRewardSummary @entity(immutable: false) {
  id: ID!
  ipaId: String
  vestingTotalAmount: BigInt!
  vestingClaimedAmount: BigInt!
  vestingStart: BigInt!
  vestingEnd: BigInt!
  vestingPendingAmount: BigInt!
  harvestCount: BigInt!
  tokenCollected: BigInt!
  tokenToIpTreasury: BigInt!
  tokenToAirdrop: BigInt!
  wipCollected: BigInt!
  wipToIpOwner: BigInt!
  wipToBuyback: BigInt!
  wipToAirdrop: BigInt!
  wipToProtocol: BigInt!
  lastUpdatedBlock: BigInt!
  lastUpdatedTimestamp: BigInt!
  # USD conversion fields
  vestingClaimedAmountUSD: BigDecimal!
  tokenCollectedUSD: BigDecimal!
  tokenToIpTreasuryUSD: BigDecimal!
  tokenToAirdropUSD: BigDecimal!
  wipCollectedUSD: BigDecimal!
  wipToIpOwnerUSD: BigDecimal!
  wipToBuybackUSD: BigDecimal!
  wipToAirdropUSD: BigDecimal!
  wipToProtocolUSD: BigDecimal!
  # Aggregate USD fields
  totalRewardsUSD: BigDecimal!
  ipOwnerRewardsUSD: BigDecimal!
  # Referral fields
  referralWipAmount: BigInt!
  referralWipAmountUSD: BigDecimal!
}

type IpRewardSummary @entity(immutable: false) {
  id: ID!
  tokenCount: BigInt!
  vestingTotalAmount: BigInt!
  vestingClaimedAmount: BigInt!
  vestingStart: BigInt!
  vestingEnd: BigInt!
  vestingPendingAmount: BigInt!
  harvestCount: BigInt!
  tokenCollected: BigInt!
  tokenToIpTreasury: BigInt!
  tokenToAirdrop: BigInt!
  wipCollected: BigInt!
  wipToIpOwner: BigInt!
  wipToBuyback: BigInt!
  wipToAirdrop: BigInt!
  wipToProtocol: BigInt!
  lastUpdatedBlock: BigInt!
  lastUpdatedTimestamp: BigInt!
  # USD conversion fields
  vestingClaimedAmountUSD: BigDecimal!
  tokenCollectedUSD: BigDecimal!
  tokenToIpTreasuryUSD: BigDecimal!
  tokenToAirdropUSD: BigDecimal!
  wipCollectedUSD: BigDecimal!
  wipToIpOwnerUSD: BigDecimal!
  wipToBuybackUSD: BigDecimal!
  wipToAirdropUSD: BigDecimal!
  wipToProtocolUSD: BigDecimal!
  # Aggregate USD fields
  totalRewardsUSD: BigDecimal!
  ipOwnerRewardsUSD: BigDecimal!
  # Referral fields
  referralWipAmount: BigInt!
  referralWipAmountUSD: BigDecimal!
}

type GlobalRewardSummary @entity(immutable: false) {
  id: ID!
  vestingTotalAmount: BigInt!
  vestingClaimedAmount: BigInt!
  vestingPendingAmount: BigInt!
  harvestCount: BigInt!
  tokenCollected: BigInt!
  tokenToIpTreasury: BigInt!
  tokenToAirdrop: BigInt!
  wipCollected: BigInt!
  wipToIpOwner: BigInt!
  wipToBuyback: BigInt!
  wipToAirdrop: BigInt!
  wipToProtocol: BigInt!
  lastUpdatedBlock: BigInt!
  lastUpdatedTimestamp: BigInt!
  # USD conversion fields
  vestingClaimedAmountUSD: BigDecimal!
  tokenCollectedUSD: BigDecimal!
  tokenToIpTreasuryUSD: BigDecimal!
  tokenToAirdropUSD: BigDecimal!
  wipCollectedUSD: BigDecimal!
  wipToIpOwnerUSD: BigDecimal!
  wipToBuybackUSD: BigDecimal!
  wipToAirdropUSD: BigDecimal!
  wipToProtocolUSD: BigDecimal!
  # Aggregate USD fields
  totalRewardsUSD: BigDecimal!
  ipOwnerRewardsUSD: BigDecimal!
  # Referral fields
  referralWipAmount: BigInt!
  referralWipAmountUSD: BigDecimal!
}

type WalletAirdropSummary @entity(immutable: false) {
  id: ID!  # wallet address
  # UGC
  ugcMemeTokenClaimed: BigInt!
  ugcMemeTokenClaimedUSD: BigDecimal!
  ugcWipClaimed: BigInt!
  ugcWipClaimedUSD: BigDecimal!
  ugcClaimCount: BigInt!
  ugcLastClaimedTimestamp: BigInt!
  # Holder
  holderMemeTokenClaimed: BigInt!
  holderMemeTokenClaimedUSD: BigDecimal!
  holderWipClaimed: BigInt!
  holderWipClaimedUSD: BigDecimal!
  holderClaimCount: BigInt!
  holderLastClaimedTimestamp: BigInt!
}

type WalletTokenAirdropSummary @entity(immutable: false) {
  # id: {wallet}-{token}
  id: ID!
  wallet: String!
  token: String!
  # UGC
  ugcMemeTokenClaimed: BigInt!
  ugcMemeTokenClaimedUSD: BigDecimal!
  ugcWipClaimed: BigInt!
  ugcWipClaimedUSD: BigDecimal!
  ugcClaimCount: BigInt!
  ugcLastClaimedTimestamp: BigInt!
  # Holder
  holderMemeTokenClaimed: BigInt!
  holderMemeTokenClaimedUSD: BigDecimal!
  holderWipClaimed: BigInt!
  holderWipClaimedUSD: BigDecimal!
  holderClaimCount: BigInt!
  holderLastClaimedTimestamp: BigInt!
}

type TokenAirdropClaimSummary @entity(immutable: false) {
  id: ID!  # token address
  # UGC claims (content creators)
  ugcMemeTokenClaimed: BigInt!
  ugcMemeTokenClaimedUSD: BigDecimal!
  ugcWipClaimed: BigInt!
  ugcWipClaimedUSD: BigDecimal!
  ugcClaimCount: BigInt!
  ugcLastClaimedTimestamp: BigInt!
  # Holder claims (token holders)
  holderMemeTokenClaimed: BigInt!
  holderMemeTokenClaimedUSD: BigDecimal!
  holderWipClaimed: BigInt!
  holderWipClaimedUSD: BigDecimal!
  holderClaimCount: BigInt!
  holderLastClaimedTimestamp: BigInt!
}

type WalletAirdropClaim @entity(immutable: true) {
  id: ID!                     # transactionHash
  wallet: String!
  token: String!
  claimType: String!          # "ugc" | "holder"
  tokenAmount: BigInt!
  wethAmount: BigInt!
  tokenAmountUSD: BigDecimal!
  wethAmountUSD: BigDecimal!
  blockNumber: BigInt!
  timestamp: BigInt!
  transactionHash: String!
}
